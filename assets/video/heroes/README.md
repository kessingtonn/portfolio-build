# Category hero clips

Optional silent loop behind a category page hero, named after the id:

    assets/video/heroes/weddings.mp4

8–15 seconds, no audio, under ~4 MB. Point `heroVideo` at it in the
matching `categories` entry. See docs/MEDIA-GUIDE.md step 3.
