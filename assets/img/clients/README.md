# Client logos

SVG where possible, otherwise transparent PNG at 2x. The strip renders
them white/greyscale, so a single-colour mark works best:

    assets/img/clients/atlas-coffee.svg

Point `logo` at it in the `clients` array in assets/js/data.js.
See docs/MEDIA-GUIDE.md step 8.
